{
  "name": "cloud_st",
  "version": "1.0.0",
  "description": "mvn clean package && docker build -t it.ciacformazione/cloud_st .",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lit-html": "^1.1.1"
  }
}
